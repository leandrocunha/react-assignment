import React from 'react';

/** very simple component to show header with the brand. */
const Header = () => (
    <div className="Header">
        <h1 className="Header__Brand">Soccer</h1>
    </div>
);

export default Header;
